token antes:struct SingleLinked {
   struct Node* head;
   struct Node* tail;
   struct Node* current;
   uint8_t size;
};
token despues:struct SingleLinked {
   struct Node* head;
   struct Node* tail;
   struct Node* current;
   uint8_t size;
};
No ha entrado!!!